# Optional. The application works without API keys.
# Set a custom cache folder if desired (defaults to ./cache).
NJ_SCHOOL_FINDER_CACHE_DIR=cache

# Optional path to an NJDOE-derived CSV/XLSX school directory.
# Supported columns are documented in README.md.
NJ_SCHOOL_DIRECTORY_FILE=

